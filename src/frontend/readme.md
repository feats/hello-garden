# Minimalistic Frontend

## Run directly

    $ npm clean-install
    $ npm test
    $ npm start

## Container

First build `../mylib`.  Then:

    $ docker build .

## Update dependencies

	$ npm install
