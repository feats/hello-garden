/* eslint-env mocha */
/* eslint-disable no-unused-expressions */
'use strict'

const chai = require('chai')
const expect = chai.expect

describe('frontend API', () => {
  it('dummy (delete me)', () => {
    expect(true).to.equal(true)
  })
  it('should handle error from backend when using invalid token')
  it('should retrieve an answer from the backend using valid token')
  it('should give the user a button to get the answer')
  it('should present the answer when the button is pressed')
  it('should give the user an opportunity to try again after each answer')
  it('should start over when user wants another go')
})
