kind: Module
name: myintegration
description: Integration tests
type: container
tests:
- name: integration
  env:
    BACKEND_USER: ${var.user}
    BACKEND_PASSWORD: ${var.password}
  dependencies:
    - backend
    - frontend
