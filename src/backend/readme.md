# Minimalistic Backend

## Run directly

    $ npm clean-install
    $ npm test
    $ npm start

## Container

First build `../lib` container.  Then:

    $ docker build .

## Update dependencies

	$ npm install
